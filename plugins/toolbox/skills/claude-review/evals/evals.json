{
  "skill_name": "claude-review",
  "evals": [
    {
      "id": 1,
      "prompt": "claude-reviewを使って $HOME/project のコードをレビューしてください。Effort は medium で。",
      "expected_output": "effort が明示されているので medium を尊重し、claude -p コマンドを --model claude-opus-5 --effort medium で実行して、対象プロジェクトのコードレビュー結果を要約・詳細・推奨対応の形式で報告する",
      "files": []
    },
    {
      "id": 2,
      "prompt": "認証周りのセキュリティについてセカンドオピニオンが欲しい。カレントディレクトリで claude-review を xhigh effort で動かして。",
      "expected_output": "effort が明示されているのでガードレール4に従い xhigh を尊重し、claude -p コマンドを --model claude-opus-5 --effort xhigh で実行する。ただし自動判定の上限は high であり xhigh 以上は精度向上に結び付かないため、その旨を一度添える。セキュリティ観点の分析結果を構造化して報告する",
      "files": []
    },
    {
      "id": 3,
      "prompt": "Claudeの別インスタンスに $HOME/project を軽くチェックしてもらいたい（low effort）",
      "expected_output": "effort が明示されているので low を尊重し、claude -p コマンドを --model claude-opus-5 --effort low で実行して、対象プロジェクトの概要レビューを報告する",
      "files": []
    },
    {
      "id": 4,
      "prompt": "claude-review で全体を見て",
      "expected_output": "effort の指定が無いので自動判定で既定の high を選ぶ（xhigh 以上は選ばない）。claude -p \"...\" --model claude-opus-5 --effort high --add-dir <dir> --permission-mode plan --disallowedTools 'Edit,Write,NotebookEdit' を Bash の run_in_background: true で起動し（ガードレール1により --permission-mode plan と --disallowedTools は両方必須）、起動直後に『バックグラウンドで開始した・数分かかる場合がある・応答が無くてもハングではない・中断不要・完了時に報告する』と予告する。完了通知で出力ファイルを Read/BashOutput して結果を要約・詳細・推奨対応の形式で報告する",
      "files": []
    },
    {
      "id": 5,
      "prompt": "（run_in_background を持たないホストで）claude-review で全体を見て",
      "expected_output": "ホストが run_in_background を持たないため foreground フォールバック（手順5）にルートする。foreground で timeout を 600000ms 確保し、起動直前に foreground 用の予告（「これからレビューを実行します…」。背景用の『バックグラウンドで開始しました』は流用しない）を出す。effort は自動判定で既定 high（xhigh 以上は選ばない）。結果は標準出力（戻り値）から取得し（背景用の出力ファイル Read/BashOutput は使わない）、要約・詳細・推奨対応の形式で報告する。タイムアウトで kill された場合は盲目的に再実行せず effort を一段下げる（high→medium）か対象を絞る",
      "files": []
    }
  ]
}
